apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-app
  namespace: argocd
spec:
  replicas: 1
  selector:
    matchLabels:
      app: test-app
  template:
    metadata:
      labels:
        app: test-app
    spec:
      containers:
      - name: mycontainer
        image: containerregistryaks1.azurecr.io/javaapplication:latest
        ports:
        - containerPort: 80

